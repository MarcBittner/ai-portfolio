# Local Kubernetes deployment (tested on kind). The image is loaded directly
# into the node (`docker save | ctr images import`), hence imagePullPolicy: Never.
# Synced by Argo from deploy/argocd/application.yaml.
#
#   kubectl apply -f deploy/k8s/agent-sandbox.yaml
---
apiVersion: v1
kind: Namespace
metadata:
  name: agent-sandbox
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-sandbox
  namespace: agent-sandbox
  labels: { app: agent-sandbox }
spec:
  replicas: 1
  selector:
    matchLabels: { app: agent-sandbox }
  template:
    metadata:
      labels: { app: agent-sandbox }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: api
          image: agent-sandbox:v0.1.0
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            requests: { cpu: 25m, memory: 64Mi }
            limits: { cpu: 500m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: agent-sandbox
  namespace: agent-sandbox
spec:
  selector: { app: agent-sandbox }
  ports:
    - port: 80
      targetPort: 8080
