"""counsel: a grounded, trust-gated personal-finance copilot.

Answers natural-language questions over a synthetic, PII-free account dataset.
Every number is computed by deterministic code; the LLM only narrates and is
held to the retrieved records (validated citations, refusal when ungrounded).
Actions are *proposed*, never executed, until a human approves them — earn
trust before agency.
"""

__version__ = "0.1.0"
