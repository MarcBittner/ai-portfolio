# Argo CD Application: doc-extract synced from the public repo.
#
#   kubectl apply -f deploy/argocd/application.yaml
#
# Assumes the image (doc-extract:v0.1.0) is already loaded into the kind node
# (docker save | ctr images import), since imagePullPolicy: Never.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: doc-extract
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/MarcBittner/ai-portfolio.git
    targetRevision: main
    path: projects/doc-extract/deploy/k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: doc-extract
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
