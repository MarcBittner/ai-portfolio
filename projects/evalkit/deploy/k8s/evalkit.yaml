# Local Kubernetes deployment (tested on kind). The image is loaded directly
# into the node (`docker save | ctr images import`), hence imagePullPolicy: Never.
# Synced by Argo from deploy/argocd/application.yaml.
#
#   kubectl apply -f deploy/k8s/evalkit.yaml
---
apiVersion: v1
kind: Namespace
metadata:
  name: evalkit
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: evalkit
  namespace: evalkit
  labels: { app: evalkit }
spec:
  replicas: 1
  selector:
    matchLabels: { app: evalkit }
  template:
    metadata:
      labels: { app: evalkit }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: api
          image: evalkit:v0.1.0
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            requests: { cpu: 25m, memory: 64Mi }
            limits: { cpu: 500m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: evalkit
  namespace: evalkit
spec:
  selector: { app: evalkit }
  ports:
    - port: 80
      targetPort: 8080
