node_modules/
.next/
out/
build/
.env
.env*.local
.vercel
# convex/_generated is committed so the Render (git-based) build has the types
# without a Convex deploy step in CI.
*.tsbuildinfo
next-env.d.ts
.convex-tmp/

# Secrets — belt-and-suspenders (none are tracked; these prevent accidental adds).
*.pem
*.key
credentials*.md
gmail-smtp.env
axiom-token.md
*secret*.env

# Session-internal working area — credentials, transcripts, and relocated legacy
# notes. NEVER tracked (secrets + process narrative). Tracked docs/spec/*.md
# (spec, development-plan, workflow, the documentation plan) and docs/audit/ DO transplant.
docs/spec/untracked/
