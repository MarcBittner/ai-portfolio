# Local Kubernetes deployment (tested on kind). The image is loaded directly
# into the node (`docker save | ctr images import`), hence imagePullPolicy: Never.
# Synced by Argo from deploy/argocd/application.yaml.
#
#   kubectl apply -f deploy/k8s/forecast.yaml
---
apiVersion: v1
kind: Namespace
metadata:
  name: forecast
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forecast
  namespace: forecast
  labels: { app: forecast }
spec:
  replicas: 1
  selector:
    matchLabels: { app: forecast }
  template:
    metadata:
      labels: { app: forecast }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: api
          image: forecast:v0.2.0
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            requests: { cpu: 25m, memory: 64Mi }
            limits: { cpu: 500m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: forecast
  namespace: forecast
spec:
  selector: { app: forecast }
  ports:
    - port: 80
      targetPort: 8080
