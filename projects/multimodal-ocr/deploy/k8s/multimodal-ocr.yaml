# Local Kubernetes deployment (tested on kind). The image is loaded directly
# into the node (`docker save | ctr images import`), hence imagePullPolicy: Never.
# Synced by Argo from deploy/argocd/application.yaml.
#
#   kubectl apply -f deploy/k8s/multimodal-ocr.yaml
---
apiVersion: v1
kind: Namespace
metadata:
  name: multimodal-ocr
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: multimodal-ocr
  namespace: multimodal-ocr
  labels: { app: multimodal-ocr }
spec:
  replicas: 1
  selector:
    matchLabels: { app: multimodal-ocr }
  template:
    metadata:
      labels: { app: multimodal-ocr }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: api
          image: multimodal-ocr:v0.1.0
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            requests: { cpu: 25m, memory: 64Mi }
            limits: { cpu: 500m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: multimodal-ocr
  namespace: multimodal-ocr
spec:
  selector: { app: multimodal-ocr }
  ports:
    - port: 80
      targetPort: 8080
