# Local Kubernetes deployment (tested on kind). The image is loaded directly
# into the node (`docker save | ctr images import`), hence imagePullPolicy: Never.
# Synced by Argo from deploy/argocd/application.yaml.
#
#   kubectl apply -f deploy/k8s/synth-data.yaml
---
apiVersion: v1
kind: Namespace
metadata:
  name: synth-data
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: synth-data
  namespace: synth-data
  labels: { app: synth-data }
spec:
  replicas: 1
  selector:
    matchLabels: { app: synth-data }
  template:
    metadata:
      labels: { app: synth-data }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: api
          image: synth-data:v0.1.0
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            requests: { cpu: 25m, memory: 64Mi }
            limits: { cpu: 500m, memory: 256Mi }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: synth-data
  namespace: synth-data
spec:
  selector: { app: synth-data }
  ports:
    - port: 80
      targetPort: 8080
